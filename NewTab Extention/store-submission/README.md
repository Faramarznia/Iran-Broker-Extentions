# فهرست بستهٔ ارسال به Chrome Web Store

تهیه‌شده در ۲۰۲۶-۰۸-۱۵ بر اساس بررسی مستقیم کد منبع فعلی (نه بر اساس اسناد قدیمی). همهٔ فایل‌های زیر در همین پوشه (`store-submission/`) هستند.

## وضعیت فایل‌ها

| فایل | وضعیت | توضیح |
|---|---|---|
| [privacy-policy.html](privacy-policy.html) | ✅ آمادهٔ ارسال | ایمیل تماس روی `iranbroker.com@gmail.com` تنظیم شد (۲۰۲۶-۰۸-۱۵) — فقط میزبانی روی URL عمومی باقی مانده (پایین) |
| [permission-justifications.md](permission-justifications.md) | ✅ آمادهٔ ارسال | permission `storage` بی‌مصرف بود، از `manifest.json` حذف شد (۲۰۲۶-۰۸-۱۵) — دیگر تصمیمی باز نمانده |
| [data-disclosure-form.md](data-disclosure-form.md) | ⚠️ نیاز به بازبینی انسانی | خودِ فایل صریحاً توصیه می‌کند یک نفر غیر از توسعه‌دهنده مرور کند — به‌خصوص بخش‌های ۳، ۵، ۶ |
| [mv3-compliance-checklist.md](mv3-compliance-checklist.md) | ✅ آمادهٔ ارسال | همهٔ ۶ مورد PASS شدند، مدرک کد پیوست است |
| [icons-review.md](icons-review.md) + [icon-preview-dark-light.png](icon-preview-dark-light.png) | ✅ آمادهٔ ارسال | هر ۴ سایز رفع شدند (۲۰۲۶-۰۸-۱۵) — ۱۶/۳۲ با `sips`، ۱۲۸ با وکتورسازی واقعی (`potrace`) از `icons/icon.svg` |
| [store-listing-fa.md](store-listing-fa.md) | ✅ آمادهٔ ارسال | عنوان و توضیح کوتاه از نظر طول کاراکتر تأیید شدند |
| [screenshots-plan.md](screenshots-plan.md) + [screenshots/](screenshots/) | ✅ آمادهٔ ارسال | هر ۵ اسکرین‌شات (۱۲۸۰×۸۰۰) تولید شدند (۲۰۲۶-۰۸-۱۵) با اسکریپت قابل‌اجرای مجدد |

---

## ⚠️ مغایرت‌ها و یافته‌های مهم

این‌ها چیزهایی هستند که در حین بررسی کد پیدا شدند و **باید عمداً تصمیم‌گیری شوند**، نه این‌که ساکت رد شوند:

### ۱. ~~permission `storage` در manifest.json کاملاً بلااستفاده است~~ ✅ رفع شد (۲۰۲۶-۰۸-۱۵)
جست‌وجوی کامل `chrome.storage.*` در هر ۸ فایل زنده صفر نتیجه داد — تمام ذخیره‌سازی محلی با `localStorage` استاندارد انجام می‌شود. بعد از تأیید مجدد (هنوز صفر مصرف‌کننده)، از `permissions` در `manifest.json` حذف شد.

### ۲. ~~آیکون‌ها همگی یک فایل ۴۸×۴۸ کپی‌شده بودند~~ ✅ رفع شد، هر ۴ سایز (۲۰۲۶-۰۸-۱۵)
`icon16.png`/`icon32.png` با `sips` downscale شدند. برای `icon128.png` — چون هیچ آرت‌ورک منبعی در ریپو نبود — شکل واقعی آیکون از پیکسل‌های `icon48.png` با `potrace` وکتوره شد (نه بزرگ‌نمایی) و یک فایل منبع دائمی در `icons/icon.svg` ذخیره شد؛ هر ۴ سایز حالا از همین یک وکتور رندر می‌شوند. جزئیات کامل در `icons-review.md`.

### ۳. کلید API سرویس GapGPT به‌صورت hardcoded در `js/airo.js` است (خط ۲۴)
این یک یافتهٔ قدیمی‌تر است (در PRD قبلی هم به‌عنوان REQ-1 ثبت شده) ولی هنوز رفع نشده — **مقدار کلید در هیچ‌کدام از فایل‌های این پوشه کپی نشده**، طبق دستور صریح شما. توصیه‌های امنیتی در `privacy-policy.html` و `data-disclosure-form.md` آمده. این یک بلاک‌کنندهٔ واقعی انتشار امن است، نه فقط یک نکتهٔ مستندسازی — چون بعد از انتشار در Chrome Web Store، هرکسی می‌تواند کد اکستنشن را دانلود و کلید را استخراج کند.

> نکته برای شفافیت: مسیر کد جایگزین (`api.anthropic.com`) در `js/airo.js` خط ۳۵ فقط یک **placeholder** (`sk-ant-REPLACE-ME`) دارد، نه یک کلید واقعی — این یکی نشتی نیست.

### ۴. یک چک‌لیست لانچ از قبل در پروژه وجود دارد (`iranbroker-launch-checklist.pdf`)
این فایل ۳۸ آیتمی، وسیع‌تر از دامنهٔ همین بستهٔ ارسال است (شامل نسخه‌گذاری manifest، rate-limiting سمت آیرا، تست fallback APIها، مانیتورینگ پس از لانچ و…). دو نکتهٔ هماهنگی با آن:
- ~~آیتم «REQ-6: بررسی PII در چت آیرا» ... گزینهٔ opt-in/عدم‌ذخیره موجود نبود~~ **رفع شد (۲۰۲۶-۰۸-۱۵)**: یک دکمهٔ قفل در هدر مودال آیرا (`#airo-privacy-btn`) «حالت خصوصی» را toggle می‌کند — وقتی فعال باشد، `persistCurrentConversation()` (`js/airo.js`) از همان ابتدا `return` می‌کند و هیچ پیامی در `localStorage` نوشته نمی‌شود؛ خودِ ترجیح کاربر (نه محتوای چت) در `ib_airo_no_history` ذخیره و بین بارگذاری‌ها حفظ می‌شود. حذف تک‌تک مکالمات هم مثل قبل باقی است. با اجرای واقعی مرورگر تست شد: toggle در هر دو جهت درست کار می‌کند، ترجیح بعد از reload کامل باقی می‌ماند، و روشن‌کردن آن مکالمات قبلاً ذخیره‌شده را پاک نمی‌کند (فقط جلوی ذخیرهٔ مکالمات بعدی را می‌گیرد).
- آن چک‌لیست دستهٔ داده را «Personally Identifiable Info و احتمالاً User Activity» پیش‌بینی کرده بود؛ در `data-disclosure-form.md` این پروژه پس از بررسی دقیق‌تر، پیام‌های چت زیر **«Personal communications»** دسته‌بندی شدند (نه User Activity، که طبق تعریف گوگل برای دادهٔ رفتاری/تله‌متری است، نه محتوای پیام) — این یک تصحیح است، نه تناقض؛ دلیلش در همان فایل توضیح داده شده.
- «REQ-2 (docs sync)» و «REQ-3 تا REQ-5» در آن چک‌لیست همگی به‌عنوان «بلاک‌کننده»/«مهم» علامت خورده بودند — این‌ها در یک نشست کاری جداگانه (همین امروز) رفع شدند: مستندات (README.md، DATA-SOURCES.md) با کد واقعی همگام شدند، ماژول‌های ES بلااستفاده حذف شدند، و باگ‌های سلکتور/کد مرده رفع شدند.

### ۵. جدول «مغایرت PRD/DATA-SOURCES.md با کد» دیگر موضوعیت ندارد
در همان نشست کاری امروز، `DATA-SOURCES.md` و `README.md` به‌طور کامل با معماری واقعی (`newtab.js` + ۷ فایل مستقل) همگام‌سازی شدند — دیگر تناقضی بین این دو سند و کد واقعی باقی نمانده. تنها سند قدیمی که هنوز نقشهٔ «فایل زنده/مرده» (Appendix A) را دارد `PRD.pdf` است که چون یک سند تاریخی/برنامه‌ریزی است، نیازی به اصلاح فوری ندارد.

---

## کارهای باقی‌مانده که فقط انسان می‌تواند انجام دهد

1. ~~ایمیل واقعی حریم خصوصی جایگزین placeholder شود~~ ✅ **رفع شد (۲۰۲۶-۰۸-۱۵)** — `iranbroker.com@gmail.com` در `privacy-policy.html` تنظیم شد.
2. ⏸️ **میزبانی `privacy-policy.html`** روی یک URL عمومی — فعلاً **on hold** طبق درخواست کاربر (۲۰۲۶-۰۸-۱۵). دو گزینه که آمادهٔ تصمیم‌اند: GitHub Pages (رایگان، خودم راه‌اندازیش می‌کنم) یا زیرمسیری از `iranbroker.net` (نیاز به دسترسی هاست دارد).
3. ~~تصمیم دربارهٔ permission `storage`~~ ✅ **رفع شد (۲۰۲۶-۰۸-۱۵)** — حذف شد.
4. **rotate/revoke کلید API فعلی GapGPT** و صدور کلید تازه قبل از انتشار عمومی — طبق چک‌لیست لانچ موجود در پروژه، این آیتم «بلاک‌کننده» است.
5. ~~آیکون ۱۲۸×۱۲۸ را با آرت‌ورک منبع واقعی جایگزین کنید~~ ✅ **رفع شد (۲۰۲۶-۰۸-۱۵)** — هر ۴ سایز حالا از `icons/icon.svg` (وکتوره‌شده با `potrace` از شکل واقعی آیکون) رندر می‌شوند. اگر تیم طراحی بعداً فایل وکتور رسمی داد، همان فایل باید جایگزین `icons/icon.svg` شود.
6. ~~گرفتن ۵ اسکرین‌شات واقعی~~ ✅ **رفع شد (۲۰۲۶-۰۸-۱۵)** — در `screenshots/` این پوشه، تولیدشده با `qa-reports/tools/09-generate-store-screenshots.js`.
7. **بازبینی مستقل داده (Data Disclosure)** توسط یک نفر غیر از توسعه‌دهنده — طبق توصیهٔ صریح در `data-disclosure-form.md`.
8. **آپلود واقعی در Developer Dashboard**: کپی‌کردن متن‌های `permission-justifications.md` و `store-listing-fa.md` در فیلدهای مربوطه، آپلود اسکرین‌شات‌ها و آیکون‌های اصلاح‌شده، تیک‌زدن فرم Data Disclosure طبق `data-disclosure-form.md`.
9. **پرداخت هزینهٔ ثبت‌نام Developer** در صورتی که حساب Chrome Web Store Developer از قبل فعال نباشد (هزینهٔ یک‌بارهٔ ۵ دلاری گوگل).
10. ~~افزودن گزینهٔ opt-in/no-store برای تاریخچهٔ چت آیرا~~ ✅ **رفع شد (۲۰۲۶-۰۸-۱۵)** — جزئیات در بخش ۴ بالا.

---

## یادداشت روش‌شناسی

هیچ عددی در این اسناد حدس زده نشده — هر ادعا (فایل، خط، نتیجهٔ `grep`/`file`/`md5`) مستقیماً از خروجی واقعی دستورهای اجراشده روی کد فعلی گرفته شده. جایی که واقعاً چیزی نامشخص بود (مثل سیاست نگهداری دادهٔ GapGPT)، به‌جای حدس زدن صریحاً «نامشخص» نوشته شده.
